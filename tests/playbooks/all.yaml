#!/usr/bin/env ansible-playbook
---

### sensible ###
# - name: All
# - description: All
# - index: 0
# - tags:
#   - test
### /sensible ###

- name: "Test"
  hosts: localhost
  connection: local
  gather_facts: true
  tasks:
    - name: "Test Task"
      shell: |
        ls ~
      register: result

    - debug: var=result

...
